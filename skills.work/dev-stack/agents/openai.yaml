# Codex UI metadata. Claude Code and Cursor use SKILL.md.
interface:
  display_name: "Dev Stack"
  short_description: "Prepare and operate the Ledidi dev stack"
  default_prompt: "Use $dev-stack to prepare only missing dependencies and run the required services or tests."
