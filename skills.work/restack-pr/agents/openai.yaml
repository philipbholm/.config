# Codex UI metadata. Claude Code and Cursor use SKILL.md.
interface:
  display_name: "Restack PR"
  short_description: "Split or restack Ledidi PRs and verify their scope"
  default_prompt: "Use $restack-pr to split or update the requested PR stack, verify both scope and behavior, and push without waiting for CI."
