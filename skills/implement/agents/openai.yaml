interface:
  display_name: "Implement"
  short_description: "Build work from a spec or tickets"
  default_prompt: "Use $implement to build this work with scoped checks and the repository’s review requirements."
policy:
  allow_implicit_invocation: false
