# SSH keys trusted to sign my own commits (git verify-commit / log --show-signature).
# Format: <principal> [options] <keytype> <key>
# This is the tracked base. install-common.sh copies it to ~/.ssh/allowed_signers,
# which is what git reads (gpg.ssh.allowedSignersFile), and install-work.sh
# appends the work key there from ~/.ssh/github_work.pub — so keys that exist on
# one machine only stay out of this file.
philipbaltzersen@gmail.com namespaces="git" ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIJ309irwc5gp1bzegL2ICuYksJDoHMJn6NV4zBPiVH5E
