# ~/.config

This is my personal config directory.

- `zsh/.zshrc` — my zsh config (symlinked to `~/.zshrc`)
- `claude/` — Claude Code config (hooks, settings, skills, agents)
- `dev/` — dev helper scripts (rebuild, shell, db, check, run-main, run-worktree, etc.)
